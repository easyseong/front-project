import React, { Component } from "react";

class FooterComponent extends Component {
  render() {
    return (
      <footer className="footer">
        <span className="text-muted">Spring Boot + JWT + React</span>
      </footer>
    );
  }
}

export default FooterComponent;
